src
    app
        user
            user.component.ts|.html
            login (cmp. folder)
            registration (cmp. folder)

        dashboard (cmp. foledr)
        
    shared
        auth.guard.ts
        pipes
            first-key.pipe.ts
        services
            auch.service.ts